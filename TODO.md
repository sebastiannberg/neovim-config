java
- check sources, is it spam or good autcomplete suggestions? text? lsp? other?
- feature to add debug adapter, already have nvim-dap, check if nvim-jdtls already has the correct keybindings for it, and connect to vespa container on port 5005
    - is it keybind to add debug breakpoints
    - where is information viewed, should be something similar to folke.toruble float workspace? research how to debug inside neovim
- code actions often show up twice
    - organize imports sometimes even show up three times and in a "random" order
- order of code actions seem random and alter depending on where I have the cursor
- code action for inserting constructor or implementing function from abstract parent insert tab characters
- code action for organizing imports do the opposite (i think) order than intellij and what chatgpt often gives
